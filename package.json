{
  "name": "api-culinary-recipes",
  "version": "1.0.0",
  "description": "Api de receitas culinárias",
  "main": "src/app.ts",
  "license": "ISC",
  "scripts": {
    "dev": "nodemon",
    "fix": "ts-standard --fix",
    "test": "echo \"Error: no test specified\" && exit 0",
    "prebuild": "rimraf ./build && yarn fix && yarn test",
    "build": "tsc",
    "start": "node build/app.js"
  },
  "husky": {
    "hooks": {
      "pre-commit": "yarn prebuild"
    }
  },
  "dependencies": {
    "compression": "^1.7.4",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/compression": "^1.7.0",
    "@types/express": "^4.17.11",
    "@types/node": "^15.3.1",
    "@typescript-eslint/eslint-plugin": "^4.24.0",
    "nodemon": "^2.0.7",
    "rimraf": "^3.0.2",
    "ts-node": "^9.1.1",
    "ts-standard": "^10.0.0",
    "tslint": "^6.1.3",
    "typescript": "^4.2.4"
  }
}